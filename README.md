# Keith Alpichi's Portfolio

## Development
On develop branch run `npm run develop` 

## Production
When ready to deploy run `npm run deploy` on the develop branch. This publishes the build source to master branch automatically
